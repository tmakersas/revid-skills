# Standalone payloads

Per-skill JSON payloads live next to each skill in `skills/<name>/examples/`.
This folder collects only the *generic* ones that don't belong to a single
skill — e.g. minimal smoke-test bodies for each workflow.
